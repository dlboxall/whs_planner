services:
  - type: web
    name: my-streamlit-app
    env: python
    buildCommand: ""
    startCommand: streamlit run app.py --server.port=$PORT --server.enableCORS=false
    envVars:
      - key: PORT
        value: 8000
